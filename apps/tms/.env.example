# MySQL connection for Better Auth + TMS tables.
# Local demo (matches docker-compose.yml). Not a production password.
DATABASE_URL=mysql://tms:tmsdemo@127.0.0.1:3306/tms

# Optional. Demo secret is used when unset — replace in production.
BETTER_AUTH_SECRET=tms-demo-secret-change-me-0123456789abcdef
BETTER_AUTH_URL=http://localhost:3000
